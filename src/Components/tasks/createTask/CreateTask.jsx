import Modal from "../../common/modal/Modal";
import TaskForm from "./TaskForm";

const CreateTask = () => {
    return (
        <Modal>
            <TaskForm/>
        </Modal>
    );
}

export default CreateTask;